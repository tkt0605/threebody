# Threebody Agent Guide

## プロジェクト構造とモジュールの構成

ThreeBodyは、最大3つのLLMからの応答を組み合わせる、Vue 3とTypeScriptを使用した音声チャットのプロトタイプです。

- `src/components/` には `VoiceSphere.vue` を含む再利用可能なUIコンポーネントを配置し、`src/views/` にはルートレベルの画面を配置します。
- `src/composables/` には、共有のリアクティブな状態や音声・チャットの動作ロジックを格納します。共有データに対して個別のコンポーネント状態を導入するのではなく、モジュールレベルのシングルトンパターンを維持してください。
- `src/lib/`、`src/types/`、`src/router/` にはユーティリティ、型定義、ルーティング設定を配置し、`public/` には静的アセットを配置します。
- `backend/routes/` にはExpressのエンドポイントを配置し、`backend/llm/providers/` にはプロバイダーのストリーミング処理を実装します。`backend/utils/` には純粋なヘルパー関数を配置します。
- テストは `src/composables/__tests__/` および `backend/tests/` に配置します。
- 変更した内容とその変更部分を具体的にまとめて、それをどのように実際に手で動かしてテストをすれば良いのかを明記。

## ビルド、テスト、開発用コマンド

リポジトリのルートディレクトリから以下のコマンドを実行します。

- `npm install` — 依存関係をインストールします。
- `npm run dev:all` — ViteとExpressを同時に起動します。
- `npm run dev` / `npm run dev:server` — フロントエンドまたはバックエンドを個別に起動します。
- `npx vite build` — フロントエンドを `dist/` にビルドします（`build` という名前のnpmスクリプトは存在しません）。
- `npm run typecheck` / `npm run typecheck:backend` — フロントエンドまたはバックエンドの型チェックを行います。
- `npm run lint` — ESLintを実行します。
- `npm test` — Vitestのテストスイートを1回実行します。

### 環境変数

- `.env` にフロント（`VITE_` 接頭辞）とバック両方の変数が同居している
- `VITE_ORIGIN_BASE_URL` は接頭辞に反してバックエンドが CORS origin として読む
- `OLLAMA_ENABLED` は未設定時 true 扱い
- モデル変更は `*_MODEL_FAST` / `_BALANCED` / `_POWERFUL` の書き換えだけで完結させる

## コーディングスタイルと命名規則

`<script setup lang="ts">` を使用したVue Composition APIを採用します。周囲のフォーマットに合わせるようにし、TypeScriptでは一般的にインデントは2スペース、引用符はシングルクォート、セミコロンは省略というスタイルを使用します。コンポーネント名はPascalCase（例: `MessageBubble.vue`）、コンポーザブルは `use` プレフィックス付き、関数や変数はcamelCaseで命名します。`any` 型の使用は避け、非同期処理（Promise）は明示的に処理してください。ESLintは型を認識するTypeScriptチェックとVueの必須ルールを適用しますが、Prettierの設定は含まれていません。

## テストのガイドライン

jsdom環境でVitestを使用し、テストファイルは指定されたディレクトリ内で `*.test.ts` という名前にします。 `npx vitest run src/composables/__tests__/useChat.test.ts` を実行して、対象を絞ったテストを行ってください。Supabaseや外部プロバイダーへの呼び出しはモック化してください。動作修正に伴うリグレッションテストを追加してください（カバレッジの閾値は設定されていません）。提出前には、関連するテスト、型チェック、およびLintを実行してください。

## コミットおよびプルリクエストのガイドライン

最近のコミットでは、`fix:`、`chore:`、`docs:` などのプレフィックスが一般的に使用されており、概要は日本語で記述されることが多いです。コミットの目的を絞り、概要を具体的に記述してください。プルリクエスト（PR）には、問題点、修正後の動作、および実施した検証内容を記載してください。また、関連するIssueへのリンクを貼り、UIの変更が視覚的に確認できる場合はスクリーンショットを添付してください。

## セキュリティと設定

認証情報（クレデンシャル）はローカルの `.env` ファイルで管理し、コミットやログには含めないようにしてください。`VITE_` で始まる変数はブラウザから参照可能であることを前提として扱ってください。共有プロバイダーのキーやSupabaseのサービス用認証情報はサーバー側で管理し、ユーザーのAPIキーについては既存のキー管理（キー・ボルト）の仕組みを維持してください。
プロンプト1 — 提案1（0章にヴィジョン、凍結表に解除条件）

# 作業: docs/CONTENT.md に「体のヴィジョン」を追記し、凍結表に解除条件を書く

このリポジトリの設計判断は `docs/CONTENT.md` 1ファイルに集約されている。今回の変更対象はこのファイルだけ。コードは触らない。新しい md ファイルも作らない（`docs/adr/` も作らない）。

## 先に読むもの
- `docs/agents/domain.md`（このファイルの読み方・書き方の規則）
- `docs/CONTENT.md` の「0. 絶対に動かさない前提」「3. 凍結」「5. 本書の更新規則」

## 守る規則（CONTENT.md 自身が定めている）
- 章番号・記号（0章 / ①②③ / 2章 …）はコードコメントから参照されている。**振り直さない、既存の見出しを変えない**
- 0章の原則そのものは書き換えない。**追記だけ**行う（5章の規則）
- 出力が0章と矛盾する場合は黙って上書きせず、報告で `[要判断]` として明示する

## 背景
0章の最上位原則は「拡散の設計原則」だけで、プロジェクトのヴィジョン（AIの体を作り、外界に触れさせ、人間との対話を円滑にする）はどこにも書かれていない。そのため「体」に必要な3つの器官——**記憶**（この人について分かっていることを持ち越す）、**手**（外界に触れる：道具・MCP・画像）、**ユーザー認識**（いま話している相手がどんな人で、どういう状態か）——が3章の凍結表で全て止まっている。凍結の解除条件は「誰が・いつ読むか」に答えること。今回はその答えを凍結表に書き込む。

## 手順

### 1. 0章に小節を追記する
「### 広がる単位の5条件」の直後、「### 到達点」の前に、次の小節を追記する。

```
### 体のヴィジョン（2026-09-05 追記）

体を持つとは、感覚器・表現器・記憶・手の4つが揃うこと。感覚器（マイク・ウェイクワード・文字）と表現器（球体・読み上げ
・バージイン）は実装済み。記憶と手が無い。

- **記憶** — この人について分かっていることを読むのは主体（`useSystemPrompt`が組むプロンプト）
- **手** — 外界に触れる。1本目は「自分の記憶  具呼び出しのループを作る）、2本目は「副体が事実を確かめる手」（検算に出典が付く）、3本目で MCP
- **ユーザー認識** — 誰か（ログインで済んでい記憶）・いまどういう状態か（`TurnSignals` /`modality`）を体が把握し、振る舞いを変える

拡散原則との関係: [あなたが決める — 例①「拡散原則が上位。体の器官は拡散の資産ではなく体の資産として別枠で進める」
例②「ヴィジョンが上位。拡散はヴィジョンの実現

記憶と手は共有される出力ではない（広がる単位  散に効くのは「副体が事実を確かめる手」だけで、そこで検算に出典が乗り、③の共有カードの中身が変わる。
```

**完了基準**: 上の小節が0章の既存の見出しを一

### 2. 3章の凍結表に「解除条件」を書く
該当4行の右列（状態 / 凍結の理由）の末尾に、次の解除条件を追記する。他の行は触らない。

| 行 | 追記する解除条件 |
|---|---|
| 操作の記録（`TurnSignals`） | 「書く側はこれ以上作らない」は維持。**読む側を足すことは凍結の対象外**（0章「体のヴィ
ジョン」ユーザー認識）。最初の読む側は読み上  要点だけ読む） |
| 好みを覚える | 解除条件: 読む側 =
主体のプロンプト（`useSystemPrompt`）。読む側ーの明示（「覚えて」）から始める |
| 外を触れる（MCP 1本） | 解除条件: 手の1本目（記憶書き込み）と2本目（副体の検索）が動いた後。読む側 = 記憶と副体 |
| 道具を増やせる | 「呼び出しの形が3種類」=   MCP。1種類目は記憶書き込み |

**完了基準**: 4行すべてに解除条件が入り、表のない。

### 3. 4章のチェックリストに1行足す
「拡散の宛先」の次に `- [ ] **体の器官** — 記憶を主体が読み、手が1本動き、`TurnSignals` を読む側が1つある`
を追加する。

### 4. 検証
- `git diff --stat` で変更が `docs/CONTENT.md` 1ファイルだけであること
- `grep -n "^## \|^# " docs/CONTENT.md` の見  こと（追記した `###` 以外）

## 報告形式
- 変更は `docs/CONTENT.md: 一行の要約` のみ。差分全文は貼らない
- 0章の既存文と矛盾すると感じた箇所は行頭 `[ 埋めない

プロンプト3 — 提案3（TurnSignals に読む側を1

# 作業: `interrupted` が続いたら、次のターン

## 背景
`src/types/intent.ts` の `TurnSignals`（`interrupted` / `rephrased` / `repeatOf`）は「推論を挟まず観測できた事実」と
して記録されているが、**読む側が存在しない** つ作る。書く側（`useChat.cancelGeneration` /`sendMessage` の rephrased 加算）は変えない。`TurnSignals`
に4個目のフィールドを足さない（型ファイルの冒）。

読み上げは `src/composables/useVoiceNarration 細は画面」の線引きは現在「コード/CLIは声に乗せない」という中身の種類だけで行い、長さでは打ち切らない（`docs/CONTENT.md`
①の決定）。今回足すのは**摩擦が観測されたとき常時の打ち切りを復活させるものではない。

## 仕様
- **判定（読む側）**: 現在の会話で、直近の assistant メッセージ2件が連続して `signals?.interrupted === true`
なら「要点モード」。判定は `src/views/ChatVie 1箇所に置く（`messages` は `useChat()`から取れる）。しきい値は定数 `BRIEF_AFTER_INTERRUPTS = 2`
- **振る舞い**: 要点モードのターンでは、読み EF_SENTENCES = 2`文までに制限する。残りは画面に出るだけで声に乗せない。`end()` でも残りを読まない
- **解除**: 割り込まれずに1ターン完了したら（pted`が立たない）自動的に通常に戻る。状態を持たず、毎ターン `computed` で判定する
- **通知**: 縮めたことを声で告げない（[あなた告げるなら「続きは画面に出しました」を1回だけ。旧仕様で撤廃した経緯が `docs/CONTENT.md` ①にある]）
- 文字入力（`TextComposer`）経路は読み上げを  響を受けない

## 実装の置き場
- `useVoiceNarration.ts`: `begin(bodyCount, lang, opts?: { maxSentences?: number })` を足す。`feed` で enqueue
した文数を数え、上限に達したら以降は enqueue する。既存の呼び出しは引数省略で挙動不変
- `ChatView.vue`: `narration.begin(...)` の呼び出し箇所（音声経路のみ）に要点モードの判定を渡す
- `useVoiceNarration.test.ts`（`src/composabl限2で3文以上を feed しても2文しか enqueueされない / `end` が残りを読まない / 上限なしは従来どおり

## 守ること
- Pinia は使わない。composables はモジュール わせる
- 読み上げ以外（マイクの占有、`syncListening`、`speechHandoff`）は触らない
- 命名・エラー処理は周辺コードに合わせる。新  報告で `[要判断]`

## 完了基準
1. 要点モードのテスト3件がグリーン
2. `npm run lint` / `npx vue-tsc --noEmit` / ` / `npm test` がすべて通る
3. `signals` の書き込み側と `intent.ts` に差分が無い（`git diff src/types/intent.ts` が空）

## 報告形式
- 変更ファイルは `パス: 一行の要約` のみ
- 確認していない関数名・型は `[要確認]`、判断が要る箇所は `[要判断]` を行頭に付ける

プロンプト2 — 提案2（副体に事実を確かめさせる手）

# 作業: 検算の副体に「事実を確かめる手」（Web検索）を1本持たせる

## 背景
三体モードは `backend/llm/textService.ts` の  担う。主体が先に答え（`answer_start` → `text` → `answer_done`）、副体がその答えを検算する（`body_start` → `body_text` → `body_done`）。副体の指示は
`backend/llm/secondaryPrompt.ts` の `buildReveviewMessages` が組み、出力は **判定 /対象箇所 / 根拠 / 代替案** の4行固定。根拠には「未確認: ○○」を書かせている。

現状、副体は閉じた知性で、「未確認」を外で確かめる手段が無い。今回、副体に1本だけ手を持たせる: **根拠に「未確認:
○○」があるとき、その1件だけを Web で検索し、  る**。

先に読むもの: `textService.ts` 全体、`secondaOTS` 〜`hasReviewFinding`、`backend/routes/chat.ts`
の経路分岐、`backend/tests/textService.test.t）。

## 設計（この形で作る）
1. **道具モジュール** `backend/tools/webSearch.ts` を新設。`searchWeb(query: string): Promise<SearchHit[]>`（`{
title, url, snippet }`）を export。実装は環境SEARCH_API_KEY` を読む単純な `fetch`（SDKは入れない）。**`SEARCH_API_URL` が未設定なら `null` を返す関数 `searchEnabled()`
を用意し、手は完全に無効**＝既存挙動と同一。
2. **2段目の呼び出し**: `orchestrateMultiBody` の副体ごとの処理で、1段目の見解（4行）を得た後に
   - `secondaryPrompt.ts` に `extractUnverifi` を足す（根拠フィールドの「未確認: ○○」から○○ を1件だけ取り出す。既存の `extractField` を再利用）
   - 取り出せて、かつ手が有効なら SSE に `{ tex, tool: 'web_search', query }`を送り、`searchWeb` を呼ぶ
   - 結果があれば同じ副体にもう1回だけ短い呼`buildVerifySystemPrompt()`（新設・`CORE_PRINCIPLES` を含める）、user は「未確認の主張」と検索結果（上位3件、snippet
300字以内）。出力は2行固定: `確認: （確認でき）` と `出典: （URL1つ。判断できずのときは「―」）`
   - この2行を `\n` で既存の見解に**追記**し  （フロントは `content += `するだけなので、追記が自然にカードへ乗る）
   - 終わったら `{ type: 'tool_done', bodyIndを送る
3. **失敗の扱い**: 検索も2段目も失敗したら見解は1段目のまま。`body_done` は既存どおり必ず送る。ログは
`sanitizeErrorMessage` を通す（キーが echo ba
4. **範囲**: `orchestrateMultiBody` に引数 `hands: { webSearch: boolean }` を足す。`routes/chat.ts`
の**共有キー経路は `false`**（運営負担のため nabled()` の結果を渡す
5. **`hasReviewFinding` は変えない**。追記された2行は判定に影響しない（既存テストで確認）
6. **SSE**: フロント `useChat.ts` の SSE ハンるので、`tool_start` / `tool_done`を送っても壊れない。今回はフロントを触らない（球体に「手を動かしている」状態を出すのは次の作業）

## 守ること
- 主体のプロンプト（`promptLayers.buildPrimarは他の体の存在を知らないまま答える設計
- 旧・統合方式のコード（`ROLE_SLOTS` / `buildSecondary*` / `buildSynthesisLayer`）は触らない。本番は `buildReview*`
側
- 副体には会話履歴を渡さない（現行どおり）。2段目にも渡さない
- 1ターンあたり副体1体につき検索1回・追加呼び
- 命名・イベント名は周辺コードに合わせる。新しいパターンは報告で `[要判断]`

## テスト（`backend/tests/`）
- `webSearch.test.ts`: 環境変数未設定で `searetch` を差し替えて `SearchHit[]` に整形される/ タイムアウトで reject
- `textService.test.ts` に追加: 手が無効なら  未確認: ○○」が無ければ検索が呼ばれない /検索成功で `tool_start` → `body_text`（確認・出典2行）→ `tool_done` → `body_done` の順 / 検索失敗でも `body_done`
が届き見解は1段目のまま / 共有キー経路（`handが呼ばれない
- `secondaryPrompt.test.ts` に追加: `extractUnverified` が根拠から1件だけ取り出す / 無ければ null

## 手動確認
`npx tsx scripts/preview-review.ts` で1往復動出典」の2行が見解の末尾に出ることを目で見る（`SEARCH_API_URL` を `.env` に置く。値はコミットしない）。`.env.example`
に `SEARCH_API_URL` / `SEARCH_API_KEY` を空値

## 完了基準
1. 上記テストがすべてグリーンで、既存テストに変更が無い
2. `npm run lint` / `npx vue-tsc --noEmit` / ` / `npm test` が通る
3. `SEARCH_API_URL` 未設定で `git stash` 前後の SSE 列が同一（テストで担保）
4. `src/` 配下に差分が無い

## 報告形式
- 変更ファイルは `パス: 一行の要約` のみ
- 検索 API の選定（Brave / SearXNG 等）は `[  挙げ、実装は URL と JSON形状を環境変数と1つのアダプタ関数に閉じる
- 確認していない関数名・型は `[要確認]`

送る順序と注意

- 1 → 3 → 2 の順。1は文書だけなので最初に済まの解除条件と照合できる
- 各プロンプトは別セッションで送る。3と2は互いに独立だが、同時に走らせると textService.ts と ChatView.vue      で衝突はしないものの、テスト件数のベースラ 222件）が二重に動く
- [あなたが決める] は2箇所（プロンプト1の拡散原則との上下関係、プロンプト3の通知の有無）。空のまま送ると Codex が推測で埋める